# Generated — rewritten verbatim by `npm run db:types`. Formatting it would
# make `npm run db:types:check` report drift on every run (Prettier adds
# clarifying parens to the generic scaffolding), hiding real schema drift.
src/lib/supabase/database.types.ts

# Supabase CLI scratch state (already gitignored)
supabase/.temp

# Playwright-generated baselines. Rewritten verbatim by `--update-snapshots`,
# so formatting them only makes the next run report a diff.
tests/e2e/*.spec.ts-snapshots

# Build and vendor output
.next
node_modules
package-lock.json
tsconfig.tsbuildinfo
test-results
playwright-report

# Approved governance artifacts — MPS, MDS, and MTS are authority.
# Never reformat them; change them only in the governing system.
mps
mds
mts
prompts
AGENTS.md
CLAUDE.md
README.md

# Vendored skill packages
.agents
.claude
skills-lock.json

# Binary / reference assets
design
public
